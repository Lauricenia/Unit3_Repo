#For the given integer X print 1 if it's positive, -1 if it's negative, or 0 if it's equal to zero.

X = int(input())

#if negative
if X > 0:
    print(1)

#if positive:
elif X < 0:
    print(-1)

else:
    print(0)
